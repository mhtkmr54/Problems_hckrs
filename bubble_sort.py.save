import random

random_items = [random.randint(-50,100) for c in range(32) ]

print "Before :",random_items
bubble_sort(random_items)
print "After :",bubble_sort

def bubble_sort







:
   for i in range(1,len(random_items)-1)
      for j in range(len(ritems)
